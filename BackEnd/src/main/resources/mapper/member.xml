<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.member.model.mapper.MemberMapper">

	<resultMap type="memberDto" id="member">
		<result column="userid" property="userid"/>
		<result column="username" property="username"/>
		<result column="userpass" property="userpass"/>
		<result column="email" property="email"/>
		<result column="birthdate" property="birthdate"/>
		<result column="gender" property="gender"/>
		<result column="address" property="address"/>
		<result column="joindate" property="joindate"/>
		<result column="salt" property="salt"/>
	</resultMap>
	
	<select id="findSaltByUserId" parameterType="String" resultType="string">
		select salt
		from member
		where userid = #{userid}
	</select>
	
	<select id="idCheck" parameterType="String" resultType="int">
		select count(userid)
		from member
		where userid = #{userid}
	</select>

	<insert id="join" parameterType="memberDto">
		insert into member (userid, username, userpass, email, salt)
		values (#{userid}, #{username}, #{userpass}, #{email}, #{salt})
	</insert>

	<select id="login" parameterType="map" resultMap="member">
		select userid, username, userpass, email, birthdate, gender, address, joindate, salt
		from member
		where userid = #{userid} and userpass = #{userpass}
	</select>
	
	<update id="modify" parameterType="memberDto">
		update member
		<trim prefix="set" suffixOverrides=",">
			<if test="email != null">email = #{email},</if>
			<if test="username != null">username = #{username},</if>
		</trim>
		where userid = #{userid}
	</update>
	
	<delete id="delete" parameterType="String">
		delete from member
		where userid = #{userid}
	</delete>
	
	<update id="modify_pw" parameterType="map">
		update member
		set userpass = #{userpass}
		where userid = #{userid}
	</update>
	
</mapper>